// Mendefinisikan panjang variable
let length = 11;

// Keluarkan nilai panjang variable
console.log(length);

// Keluarkan luas lingkaran menggunakan variabel Panjang
console.log(Math.PI * length * length);
